religious = {
	allowed_for_character = {
		OR = { # Cannot Holy War if your Liege(s) are of a different Faith than you.
			is_independent_ruler = yes
			any_liege_or_above = {
				faith = prev.faith
				count = all
			}
		}
		faith = {
			NOR = {
				has_doctrine_parameter = unreformed # Unreformed Pagans cannot Holy War
				AND = {
					has_doctrine_parameter = holy_wars_forbidden # Pacifists cannot Holy War
					NOT = { prev = { has_character_flag = bonus_major_holy_war } } #...unless they got the legend bonus
				}
			}
		}
		herders_and_tributary_constraints = yes
		#Game rules
		OR = {
			any_player = { this = root }
			NOT = { has_game_rule = no_ai_aggressiveness }
		}
		OR = {
			is_independent_ruler = yes
			has_game_rule = default_external_vw
			AND = {
				has_game_rule = player_external_vw
				is_ai = no
			}
		}
		OR = {
			has_game_rule = holy_war_cb_default
			NOT = { has_character_modifier = mgr_recent_holy_war }
		}
	}
}

#Holy wars that should be restricted if your liege disagrees, or your faith does, but which don't require organised religion. E.g., human sacrifice raids.
religious_disorganised = {
	allowed_for_character = {
		OR = { # Cannot Holy War if your Liege(s) are of a different Faith than you.
			is_independent_ruler = yes
			any_liege_or_above = {
				faith = prev.faith
				count = all
			}
		}
		faith = {
			NOT = { has_doctrine_parameter = holy_wars_forbidden }	# Pacifists cannot Holy War
		}
		herders_and_tributary_constraints = yes
	}
}

religious_script_only = {
	should_check_for_interface_availability = no
	can_only_start_via_script = yes # Only fired by event
}

de_jure = {
	allowed_for_character = {
		herders_and_tributary_constraints = yes
		#Game rules
		OR = {
			any_player = { this = root }
			NOT = { has_game_rule = no_ai_aggressiveness }
		}
		OR = {
			is_independent_ruler = yes
			has_game_rule = default_external_vw
			has_game_rule = de_jure_external_vw
			AND = {
				has_game_rule = player_external_vw
				is_ai = no
			}
		}
	}
}

claim = {
	allowed_for_character = {
		herders_and_tributary_constraints = yes
		#Game rules
		OR = {
			any_player = { this = root }
			NOT = { has_game_rule = no_ai_aggressiveness }
		}
		OR = {
			is_independent_ruler = yes
			has_game_rule = default_external_vw
			AND = {
				has_game_rule = player_external_vw
				is_ai = no
			}
		}
	}
}

civil_war = {
	should_check_for_interface_availability = no
	can_only_start_via_script = yes # Only fired by event
	allowed_for_character = {
		herders_and_tributary_constraints = yes
	}
}

invasion = {
	allowed_for_character = {
		herders_and_tributary_constraints = yes
		#Game rules
		OR = {
			any_player = { this = root }
			NOT = { has_game_rule = no_ai_aggressiveness }
		}
		OR = {
			is_independent_ruler = yes
			has_game_rule = default_external_vw
			AND = {
				has_game_rule = player_external_vw
				is_ai = no
			}
		}
		OR = {
			has_game_rule = conquest_war_cb_default
			NOT = { has_character_modifier = mgr_recent_conquest_war }
		}
	}
}

vassalization = {
	allowed_for_character = {
		herders_and_tributary_constraints = yes
		#Game rules
		OR = {
			any_player = { this = root }
			NOT = { has_game_rule = no_ai_aggressiveness }
		}
		OR = {
			is_independent_ruler = yes
			has_game_rule = default_external_vw
			AND = {
				has_game_rule = player_external_vw
				is_ai = no
			}
		}
	}
}

conquest = {
	allowed_for_character = {
		herders_and_tributary_constraints = yes
		#Game rules
		OR = {
			any_player = { this = root }
			NOT = { has_game_rule = no_ai_aggressiveness }
		}
		OR = {
			is_independent_ruler = yes
			has_game_rule = default_external_vw
			AND = {
				has_game_rule = player_external_vw
				is_ai = no
			}
		}
		OR = {
			has_game_rule = conquest_war_cb_default
			NOT = { has_character_modifier = mgr_recent_conquest_war }
		}
	}
}

struggle = {
	allowed_for_character = { 
		can_use_struggle_cbs_trigger = yes 
		herders_and_tributary_constraints = yes
		#Game rules
		OR = {
			any_player = { this = root }
			NOT = { has_game_rule = no_ai_aggressiveness }
		}
		OR = {
			is_independent_ruler = yes
			has_game_rule = default_external_vw
			AND = {
				has_game_rule = player_external_vw
				is_ai = no
			}
		}
	}
}

subjugation = {
	allowed_for_character = {
		herders_and_tributary_constraints = yes
		#Game rules
		OR = {
			any_player = { this = root }
			NOT = { has_game_rule = no_ai_aggressiveness }
		}
		OR = {
			is_independent_ruler = yes
			has_game_rule = default_external_vw
			AND = {
				has_game_rule = player_external_vw
				is_ai = no
			}
		}
		OR = {
			has_game_rule = conquest_war_cb_default
			NOT = { has_character_modifier = mgr_recent_conquest_war }
		}
	}
}

independence = {
	allowed_for_character = {
		#Game rules
		OR = {
			is_ai = no
			NOT = { has_game_rule = no_ai_aggressiveness }
		}
	}
}

artifact = {
	allowed_for_character = {
		herders_and_tributary_constraints = yes
		#Game rules
		OR = {
			any_player = { this = root }
			NOT = { has_game_rule = no_ai_aggressiveness }
		}
		OR = {
			is_independent_ruler = yes
			has_game_rule = default_external_vw
			AND = {
				has_game_rule = player_external_vw
				any_player = { this = root }
			}
		}
	}
}

humiliation = {
	allowed_for_character = {
		herders_and_tributary_constraints = yes
		#Game rules
		OR = {
			any_player = { this = root }
			NOT = { has_game_rule = no_ai_aggressiveness }
		}
		OR = {
			is_independent_ruler = yes
			has_game_rule = default_external_vw
			AND = {
				has_game_rule = player_external_vw
				any_player = { this = root }
			}
		}		
	}
}